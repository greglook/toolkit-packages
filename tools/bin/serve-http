#!/bin/sh

python -m SimpleHTTPServer ${1:-3003}

#!/usr/bin/env ruby
# vim: ft=ruby
#require 'webrick'
#server = WEBrick::HTTPServer.new :Port => 3003
#server.mount '/', WEBrick::HTTPServlet::FileHandler, './'
#trap('INT') { server.stop }
#server.start
